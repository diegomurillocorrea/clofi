/** Clofi payroll is always denominated in US dollars. */
export const CLOFI_PAYROLL_CURRENCY = 'USD' as const

/** Solo empleados con este rol (public.roles.slug) se muestran en Clofi. */
export const CLOFI_EMPLOYEE_ROLE_SLUG = 'vendedor' as const
